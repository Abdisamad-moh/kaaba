{# <div {{ attributes }}>
	{{ form_start(form, {attr: {
        'data-action': 'live#action:prevent',
        'data-live-action-param': 'save'
    }}) }}
	<div class="modal-body">
		<div class="row pb-3">
			<div class="col-lg-12">
				<label class="form-label">Full Name
				</label>
				{{ form_widget(form.name, {attr: {'class': 'form-control', 'placeholder': 'Type Your Full Name'}}) }}
			</div>
		</div>
		<div class="row pb-3">
			<div class="col-lg-12">
				<label class="form-label">Email Address</label>
				{{ form_widget(form.email, {attr: {'class': 'form-control', 'placeholder':'Type Your Email Address'}}) }}
			</div>
		</div>
		<div class="row pb-3">
			<div class="col-lg-6">
				<label for="form-label">Gender</label>
				{{ form_widget(form.gender, {attr: {'class': 'form-select'}}) }}
			</div>
			<div class="col-lg-6">
				<label for="form-label">DOB</label>
				{{ form_widget(form.dob, {attr: {'class': 'form-control'}}) }}
			</div>
		</div>
		<div class="row pb-3">
			<div class="col-lg-6">
				<label for="form-label">Phone Number</label>
				{{ form_widget(form.phone, {attr: {'class': 'form-control', 'placeholder': 'e.g. +252 63 773485'}}) }}
			</div>
			<div class="col-lg-6">
				<label for="form-label">Whatsapp Number</label>
				{{ form_widget(form.whatsappPhone, {attr: {'class': 'form-control', 'data-jobseeker-profile-target': 'phones', 'placeholder': 'e.g. +252 63 773485'}}) }}
			</div>
		</div>
		<div class="row pb-3">
			<div class="col-lg-6">
				<label class="form-label">Country
				</label>
				{{ form_widget(form.country) }}
			</div>
			<div class="col-lg-6">
				<label class="form-label">City
				</label>
				{{ form_widget(form.city) }}
			</div>
		</div>
		<div class="row pb-3">
			<div class="col-lg-12">
				<label for="" class="form-label">Location</label>
				{{ form_widget(form.location, {attr: {'class': 'form-control', 'placeholder':'Type Your Location'}}) }}
			</div>
		</div>
		<div class="modal-footer">
			<a href="#" class="btn btn-outline-primary" style="border-radius: 50px;" data-bs-dismiss="modal">Cancel</a>
			<button type="submit" class="btn btn-primary waves-effect waves-light">Save</button>
		</div>
	</div>
	<div class="d-flex justify-content-end gap-2">
		{{ form_widget(form.jobSeekerWorks.vars.button_add, {label: '+ Add New', attr: {class: 'btn btn-outline-primary'}}) }}
		<button class="btn btn-primary {{ this.hasValidationErrors ? 'disabled' : '' }}">Save</button>
	</div>
	{{ form_end(form) }}
</div> #}
