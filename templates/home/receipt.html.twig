<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Receipt - Metier Quest</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
		<style>
			body {
				background-color: #f8f9fa;

			}

			.receipt {
				background-color: #ffffff;
				border-radius: 8px;
				padding: 30px;
				max-width: 600px;
				margin: 50px auto;
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
			}

			.receipt-header {
				border-bottom: 2px solid #e9ecef;
				padding-bottom: 20px;
				margin-bottom: 20px;
			}

			.receipt-header h4 {
				margin: 0;
				font-weight: bold;
				color: #343a40;
			}

			.receipt-header small {
				color: #6c757d;
			}

			.receipt-table th,
			.receipt-table td {
				padding: 12px;
				vertical-align: middle;
			}

			.receipt-table th {
				color: #495057;
				background-color: #e9ecef;
				border-top: 1px solid #e9ecef;
			}

			.receipt-summary {
				background-color: #f8f9fa;
				padding: 20px;
				border-radius: 8px;
				margin-top: 20px;
			}

			.receipt-summary th,
			.receipt-summary td {
				padding: 8px;
				border: none;
			}

			.receipt-summary th {
				color: #495057;
			}

			.receipt-summary td {
				font-weight: bold;
				color: #343a40;
			}

			.btn-print {
				margin-top: 20px;
				text-align: center;
				display: none; /* Hide print button by default */
			}

			@media print {
				.btn-print {
					display: none; /* Hide print button again in print mode */
				}

				.receipt {
					page-break-before: avoid; /* Avoid breaking content across pages */
				}
			}
		</style>
	</head>

	<body>

		<div class="receipt">
			<div class="receipt-header text-center">
				<img src="https://metier-quest.com/assets/images/logo-dark.png" alt="Metier Quest Logo"/>
				<h4>Metier Quest</h4>
				<small>Receipt</small>
			</div>
			<div class="row mb-4">
				<div class="col-md-6">
					<h6>Billed To</h6>
					<p>{{order.customer.name}}<br>
						{% if order.category == "jobseeker" %}
						{{order.customer.jobseekerdetails.country.name}},
                        {{order.customer.jobseekerdetails.city.name}}
                        {% else %}
                        {{order.customer.employerdetails.country.name}},
                        {{order.customer.employerdetails.city.name}}
                        {% endif %}
                        
                        <br>
						{{order.customer.email}}
					</p>
				</div>
						{% set tax = ((order.plan.cost * 5) / 100) | round(2) %}
			{% set final = (order.plan.cost + tax) | round(2) %}
				<div class="col-md-6 text-md-end">
					<h6>Order Details</h6>
					<p>
						Order Number:
						<strong>MQ-{{order.id}}</strong><br>
						Date:
						<strong>{{order.orderdate|date("d-m-Y")}}</strong>
					</p>
				</div>
			</div>
			<table class="table receipt-table">
				<thead>
					<tr>
						<th>Description</th>
						<th class="text-end">Quantity</th>
						<th class="text-end">Price</th>
						<th class="text-end">Amount</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td>{{order.plan.name}}</td>
						<td class="text-end">1</td>
						<td class="text-end">${{order.plan.cost}}</td>
						<td class="text-end">${{order.plan.cost}}</td>
					</tr>
				</tbody>
			</table>
			<div class="receipt-summary">
				<table class="table">
					<tbody>
						<tr>
							<th class="text-end">Subtotal</th>
							<td class="text-end">${{order.plan.cost}}</td>
						</tr>
						<tr>
							<th class="text-end">Tax</th>
							<td class="text-end">${{tax}}</td>
						</tr>
						<tr>
							<th class="text-end">Total</th>
							<td class="text-end">${{order.plan.cost + tax}}</td>
						</tr>
					</tbody>
				</table>
			</div>

			<div class="btn-print">
				<button onclick="window.print();" class="btn btn-primary">Print Receipt</button>
			</div>
		</div>
       
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

		<script>
			window.print();
		</script>
		

	</body>

</html>
